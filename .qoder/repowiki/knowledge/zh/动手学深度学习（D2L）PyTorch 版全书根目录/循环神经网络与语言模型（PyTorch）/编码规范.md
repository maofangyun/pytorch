- 所有代码单元格通过 `tab: [pytorch]` 元数据标记为 PyTorch 实现，便于多框架版本生成。
- 可复用的函数和数据集定义使用 `#@save` 注释导出到 d2l 命名空间，供其他 notebook 直接调用。
- 文本处理流程统一遵循“读取→分词→构建词表→索引化”四步，并在各 notebook 中重复该模式。
- 小批量序列输入形状约定为 `(时间步, 批量大小, 词表大小)`，通过转置 one-hot 编码获得。
- 数学公式以 LaTeX 行内/块级形式嵌入 Markdown 单元格，配合 `:label:` 和 `:numref:` 交叉引用。