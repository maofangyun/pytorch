- 每个 notebook 开头统一使用 `%matplotlib inline` 并导入 `torch`、`torchvision`、`torch.nn` 以及 `from d2l import torch as d2l`。
- Markdown 单元格通过 `:label:`sec_xxx`` 和 `:numref:`sec_xxx`` 交叉引用其他小节，形成文档级链接。
- 代码单元格通过 metadata 中的 `tab: ["pytorch"]` 标记框架实现，便于多框架同步生成。
- 模型构建普遍采用先加载 ImageNet 预训练的 ResNet，再裁剪最后的全局平均池化与全连接层替换为任务头（如 1×1 卷积 + 转置卷积）的微调范式。