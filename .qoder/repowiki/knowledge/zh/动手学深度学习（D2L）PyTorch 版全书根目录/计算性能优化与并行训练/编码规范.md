- 所有代码单元格通过`tab: [pytorch]`元数据标记为PyTorch实现，便于多框架对比渲染
- 性能基准统一使用`d2l.Benchmark(name)`上下文管理器包裹待测代码块，并在必要时调用`torch.cuda.synchronize(device)`确保计时准确
- 设备获取通过`d2l.try_gpu()`和`d2l.try_all_gpus()`抽象，避免硬编码CUDA设备索引
- 模型定义与训练循环分离：先定义简单网络（如LeNet），再逐步添加并行/同步逻辑
- Markdown单元格中使用`:label:`sec_xxx``格式为章节生成交叉引用标签