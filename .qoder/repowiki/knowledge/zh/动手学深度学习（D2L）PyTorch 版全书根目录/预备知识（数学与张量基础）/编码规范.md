- 每个 notebook 开头用 markdown 单元格定义标题与 `:label:` 标签，便于后续交叉引用。
- 代码单元格通过 metadata 中的 `tab: ["pytorch"]` 声明所属框架，支持多框架并行展示。
- 教学函数与绘图工具通过 `#@save` 注释交由 `d2l` 包统一保存，避免重复定义。
- 数学公式统一使用 LaTeX 语法嵌入 markdown，变量命名遵循书中约定（如小写斜体表示标量、粗体表示向量/矩阵）。