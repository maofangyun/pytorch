- 每个notebook以markdown单元格开头，使用`:label:`命名锚点（如`sec_bert`、`subsec_bert_input_rep`）以便跨notebook引用
- 关键函数和类通过`#@save`装饰器标记，使其可从notebook外部导入复用
- 代码单元格通过`tab: ["pytorch"]`元数据声明框架标签，便于多框架同步
- 数学公式统一使用LaTeX语法嵌入markdown，并通过`:eqlabel:`为公式添加编号引用
- 图像引用统一使用相对路径`../img/文件名.svg`格式