- 每个子章节使用独立的 .ipynb 文件并以 # 标题 + :label:`sec_xxx` 的形式定义可交叉引用的节标签
- 正文以 Markdown 单元格为主，关键操作步骤以 ```bash 或 ```python 代码块呈现，便于读者直接复制执行
- 图片统一通过 ../img/ 相对路径引用，并配合 :width: 和 :label: 元数据保持排版一致
- 通过 :ref:`chap_xxx` 和 :numref:`fig_xxx`/`:tab_xxx` 实现跨章节与跨图表的引用，而非硬编码链接