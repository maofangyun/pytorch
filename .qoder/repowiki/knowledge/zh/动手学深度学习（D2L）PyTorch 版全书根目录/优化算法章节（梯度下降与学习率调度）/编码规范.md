- 每个 Notebook 以 Markdown 单元格开头，使用 `:label:`sec_xxx`` 定义节标签，便于跨 Notebook 引用。
- 代码单元通过元数据 `tab: ["pytorch"]` 标记框架版本，统一导入 `import torch` 和 `from d2l import torch as d2l`。
- 目标函数与其梯度分别定义为同名 `f` 和 `f_grad` 函数，保持接口一致以便在多个优化算法中复用。
- 优化迭代过程封装为独立函数（如 `gd`、`sgd`），返回参数轨迹列表供后续绘图展示收敛过程。
- 使用 `results.append(float(x))` 记录每步参数值，配合 Matplotlib 绘制二维或一维优化轨迹图。