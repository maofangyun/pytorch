- 所有可复用模型类均使用`#@save`装饰器标记以便导出，并继承`nn.Module`或自定义`d2l.Decoder`基类
- 张量形状在注释中以`(batch_size, num_steps, num_hiddens)`等形式显式标注，确保维度变换可追踪
- 注意力权重通过实例属性`self._attention_weights`收集并在`attention_weights` property中暴露，便于可视化对齐结果
- 每个notebook统一以`import torch; from torch import nn; from d2l import torch as d2l`开头，保持依赖一致性
- 数学公式使用LaTeX语法配合`:eqref:`和`:numref:`进行交叉引用，图表通过`:label:`命名供正文引用